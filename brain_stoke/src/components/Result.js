// src/components/Result.js
import React from 'react';

const Result = ({ result }) => {
    return (
        <div>
            <h2>Prediction Result: {result}</h2>
        </div>
    );
};

export default Result;